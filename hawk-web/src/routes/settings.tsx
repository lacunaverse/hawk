import { Nav } from '../components/layouts/Nav';
import '../main.css';

export const Settings = () => {
    document.title = 'Settings';
    return (
        <div className="main">
            <Nav />
        </div>
    );
};
